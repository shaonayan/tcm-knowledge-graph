# 中医知识图谱 - 部署总结

## 📊 项目状态检查

### ✅ 已完成的工作

1. **项目结构**
   - ✅ 前端：React + Vite + TypeScript
   - ✅ 后端：Node.js + Express + TypeScript
   - ✅ 数据库：Neo4j Aura
   - ✅ Docker 配置：docker-compose.yml + Dockerfiles

2. **部署配置文件**
   - ✅ `vercel.json` - Vercel 前端部署配置
   - ✅ `frontend/vercel.json` - 前端 SPA 路由配置
   - ✅ `frontend/Dockerfile` - 前端 Docker 镜像
   - ✅ `backend/Dockerfile` - 后端 Docker 镜像
   - ✅ `docker-compose.yml` - Docker Compose 配置
   - ✅ `frontend/nginx.conf` - Nginx 配置（Docker 使用）

3. **环境配置**
   - ✅ `backend/env.example` - 后端环境变量示例
   - ✅ API 地址自动配置（`frontend/src/services/api.ts`）

---

## 🚀 推荐部署方案

根据项目配置，推荐以下部署架构：

### 方案一：云平台部署（推荐）

```
前端 (Vercel) → 后端 (Render) → Neo4j Aura
```

**优势**：
- ✅ 免费套餐可用
- ✅ 自动 HTTPS
- ✅ 自动部署（GitHub 推送）
- ✅ CDN 加速（Vercel）
- ✅ 易于维护

**部署位置**：
- **前端**: [Vercel](https://vercel.com) - 静态网站托管
- **后端**: [Render](https://render.com) - Node.js Web Service
- **数据库**: [Neo4j Aura](https://neo4j.com/cloud/aura/) - 托管图数据库

### 方案二：Docker 部署

```
Docker Compose → 本地服务器/VPS
```

**优势**：
- ✅ 完全控制
- ✅ 适合私有部署
- ✅ 统一管理

**部署位置**：
- 任何支持 Docker 的服务器
- 云服务器（阿里云、腾讯云、AWS 等）

---

## 📋 部署检查清单

### 前端部署（Vercel）

- [ ] GitHub 仓库已连接
- [ ] Root Directory 设置为 `frontend`
- [ ] Build Command: `npm run build` 或 `pnpm build`
- [ ] Output Directory: `dist`
- [ ] 环境变量 `VITE_API_BASE_URL` 已设置（后端 URL + `/api`）

### 后端部署（Render）

- [ ] GitHub 仓库已连接
- [ ] Root Directory 设置为 `backend`
- [ ] Build Command: `npm install && npm run build`
- [ ] Start Command: `npm start`
- [ ] 环境变量已配置：
  - [ ] `NEO4J_URI`
  - [ ] `NEO4J_USER`
  - [ ] `NEO4J_PASSWORD`
  - [ ] `CORS_ORIGIN`（前端域名）
  - [ ] `JWT_SECRET`

### 数据库（Neo4j Aura）

- [ ] 数据库实例已创建
- [ ] 连接信息已记录
- [ ] 数据已导入（运行 `npm run import:tcm`）

---

## 🔍 与 GitHub 同步状态

### 当前状态

**已暂存的更改**：
- 大量旧文档和配置文件已标记删除（待提交）
- 新增 `frontend/.env` 文件

**未暂存的更改**：
- 多个源代码文件已修改
- 新增多个配置文件（Docker、nginx 等）

**未跟踪的文件**：
- `.env.example`
- `.gitignore`
- `README.md`
- `DEPLOYMENT.md`
- Docker 相关文件
- 其他配置文件

### 建议操作

1. **提交所有更改**：
   ```bash
   git add .
   git commit -m "整理项目结构，添加部署配置"
   git push origin main
   ```

2. **检查 `.gitignore`**：
   - 确保 `.env` 文件被忽略
   - 确保 `node_modules/` 被忽略
   - 确保 `dist/` 和 `build/` 被忽略

---

## 📝 部署步骤摘要

### 1. 准备数据库（Neo4j Aura）
```bash
# 创建 Aura 实例
# 记录连接信息
# 导入数据
cd backend
npm run import:tcm
```

### 2. 部署后端（Render）
```
1. 连接 GitHub 仓库
2. Root Directory: backend
3. Build: npm install && npm run build
4. Start: npm start
5. 配置环境变量
6. 部署
```

### 3. 部署前端（Vercel）
```
1. 连接 GitHub 仓库
2. Root Directory: frontend
3. Build: npm run build
4. Output: dist
5. 设置 VITE_API_BASE_URL
6. 部署
```

### 4. 更新 CORS
```
后端环境变量中更新 CORS_ORIGIN 为前端域名
重新部署后端
```

---

## ⚠️ 注意事项

1. **环境变量**
   - 不要将 `.env` 文件提交到 Git
   - 使用各平台的环境变量配置功能

2. **API 地址**
   - 前端默认生产环境使用：`https://tcm-knowledge-graph.onrender.com/api`
   - 可通过环境变量 `VITE_API_BASE_URL` 覆盖

3. **CORS 配置**
   - 后端必须配置正确的前端域名
   - 部署前端后记得更新后端 CORS

4. **Render 免费套餐**
   - 服务会在 15 分钟无活动后休眠
   - 首次访问需要等待 30-60 秒唤醒

---

## 📚 相关文档

- [详细部署指南](./DEPLOYMENT.md)
- [项目 README](./README.md)
- [Vercel 文档](https://vercel.com/docs)
- [Render 文档](https://render.com/docs)

---

**最后更新**: 2024年

