# 中医知识图谱Web应用

## 🎯 项目简介

这是一个基于Neo4j图数据库的中医知识图谱Web应用，旨在为中医教学、临床诊断和学术研究提供现代化的数字工具。

## 📊 项目规模

- **节点数量**: 6,820个中医术语节点
- **关系数量**: 43,542个层次关系
- **分类体系**: 17个主要根分类
- **数据来源**: 中医病证分类与代码国家标准

## 🏗️ 技术架构

### 前端技术栈
- **框架**: React 18 + TypeScript
- **构建工具**: Vite
- **UI组件**: Ant Design + Tailwind CSS
- **可视化**: D3.js + Cytoscape.js + ECharts
- **状态管理**: Zustand
- **路由**: React Router v6

### 后端技术栈
- **运行时**: Node.js + Express
- **语言**: TypeScript
- **数据库**: Neo4j + Redis (缓存)
- **API**: REST API
- **认证**: JWT + Passport.js
- **文档**: Swagger/OpenAPI

## 📁 项目结构

```
tcm-knowledge-graph/
├── frontend/           # React前端应用
│   ├── public/        # 静态资源
│   └── src/           # 源代码
│       ├── components/ # 可复用组件
│       ├── pages/     # 页面组件
│       ├── hooks/     # 自定义Hooks
│       ├── services/  # API服务
│       ├── stores/    # 状态管理
│       ├── utils/     # 工具函数
│       ├── styles/    # 样式文件
│       └── types/     # 类型定义
├── backend/           # Node.js后端API
│   └── src/          # 源代码
│       ├── controllers/ # 控制器
│       ├── services/   # 业务逻辑
│       ├── routes/     # 路由定义
│       ├── middleware/ # 中间件
│       ├── models/     # 数据模型
│       ├── utils/      # 工具函数
│       ├── config/     # 配置文件
│       └── types/      # 类型定义
└── docs/              # 项目文档
```

## 🚀 快速开始

### 环境要求

- Node.js >= 18.0.0
- npm >= 8.0.0
- Neo4j >= 5.0.0
- Redis >= 6.0.0

### 安装依赖

```bash
# 克隆项目
git clone <repository-url>
cd tcm-knowledge-graph

# 安装前端依赖
cd frontend
npm install

# 安装后端依赖
cd ../backend
npm install
```

### 环境配置

1. 复制环境配置文件：
```bash
# 后端环境配置
cp backend/.env.example backend/.env
```

2. 配置Neo4j连接信息：
```env
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_password
```

3. 配置Redis连接信息：
```env
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password
```

### 启动开发服务器

```bash
# 启动后端服务器
cd backend
npm run dev

# 启动前端开发服务器
cd frontend
npm run dev
```

## 🎨 核心功能

### 1. 首页仪表板
- 快速统计概览
- 智能搜索入口
- 分类分布可视化
- 推荐探索内容

### 2. 图谱探索器
- 交互式知识图谱浏览
- 多层级节点展开
- 实时节点详情展示
- 多种布局算法

### 3. 智能搜索
- 全文搜索功能
- 高级筛选选项
- 搜索结果高亮
- 相关推荐系统

### 4. 数据分析
- 多维度统计分析
- 交互式图表展示
- 数据导出功能
- 质量评估报告

### 5. 学习模块
- 个性化学习路径
- 知识点可视化
- 在线测试系统
- 学习进度跟踪

## 🔗 Neo4j数据结构

### 节点属性
- `代码`: 标准分类代码
- `分类级别`: 层次级别
- `主期`: 主要术语名称
- `类别`: 疾病类/证候类
- `同义词1-5`: 相关同义词

### 关系类型
- `包含`: 表示层次包含关系

### 示例查询
```cypher
// 查询外感病类的完整层次结构
MATCH path = (root)-[:包含*0..3]->(child)
WHERE toString(root[keys(root)[0]]) = "A01."
RETURN path
```

## 📖 API文档

启动后端服务器后，访问 `http://localhost:3000/api-docs` 查看完整的API文档。

## 🛠️ 开发指南

### 代码规范
- 使用ESLint + Prettier进行代码格式化
- 遵循TypeScript严格模式
- 组件采用函数式编程风格
- API接口遵循RESTful设计原则

### 提交规范
```bash
# 功能开发
git commit -m "feat: 添加图谱可视化组件"

# 问题修复
git commit -m "fix: 修复搜索结果分页问题"

# 文档更新
git commit -m "docs: 更新API文档"
```

## 🧪 测试

```bash
# 前端测试
cd frontend
npm run test

# 后端测试
cd backend
npm run test

# E2E测试
npm run test:e2e
```

## 📦 部署

### Docker部署
```bash
# 构建镜像
docker-compose build

# 启动服务
docker-compose up -d
```

### 生产环境部署
```bash
# 构建前端
cd frontend
npm run build

# 构建后端
cd backend
npm run build

# 启动生产服务器
npm start
```

## 🤝 贡献指南

1. Fork项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开Pull Request

## 📄 许可证

本项目采用MIT许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 👥 团队

- **项目负责人**: [您的姓名]
- **技术架构**: 基于现代Web技术栈
- **数据来源**: 中医病证分类与代码国家标准

## 📞 联系方式

如有问题或建议，请通过以下方式联系：

- 📧 邮箱: [您的邮箱]
- 🐛 问题反馈: [GitHub Issues]
- 💬 讨论: [GitHub Discussions]

## 🙏 致谢

感谢所有为中医标准化和数字化做出贡献的专家学者，以及开源社区提供的优秀工具和框架。
