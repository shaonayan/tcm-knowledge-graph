# 如何浏览中医知识图谱网站

## 🌐 方法一：本地运行（推荐用于开发测试）

### 步骤 1：安装依赖

打开 PowerShell 或命令提示符，进入项目目录：

```powershell
cd C:\Users\31600\Desktop\tcm-knowledge-graph\frontend
```

安装前端依赖：

```powershell
npm install
```

> **注意**：如果还没有安装 Node.js，请先访问 https://nodejs.org/ 下载安装（需要 Node.js >= 18.0.0）

### 步骤 2：启动开发服务器

```powershell
npm run dev
```

启动成功后，你会看到类似以下输出：

```
  VITE v5.0.8  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

### 步骤 3：在浏览器中打开

复制显示的地址（通常是 `http://localhost:5173/`），在浏览器中打开即可浏览网站。

---

## 🚀 方法二：构建生产版本并预览

如果你想查看生产构建的效果：

### 步骤 1：构建项目

```powershell
cd C:\Users\31600\Desktop\tcm-knowledge-graph\frontend
npm run build
```

### 步骤 2：预览构建结果

```powershell
npm run preview
```

然后在浏览器中访问显示的地址（通常是 `http://localhost:4173/`）

---

## 🌍 方法三：部署到 GitHub Pages（在线访问）

如果你想让其他人也能访问你的网站，可以部署到 GitHub Pages：

### 步骤 1：安装 GitHub Pages 部署工具

```powershell
cd C:\Users\31600\Desktop\tcm-knowledge-graph\frontend
npm install --save-dev gh-pages
```

### 步骤 2：修改 package.json

在 `package.json` 的 `scripts` 部分添加：

```json
"deploy": "npm run build && gh-pages -d dist"
```

### 步骤 3：配置 Vite

在 `vite.config.ts` 中添加 `base` 配置：

```typescript
export default defineConfig({
  base: '/tcm-knowledge-graph/',
  // ... 其他配置
})
```

### 步骤 4：部署

```powershell
npm run deploy
```

部署完成后，网站将可以通过以下地址访问：
```
https://shaonayan.github.io/tcm-knowledge-graph/
```

---

## 📋 快速启动脚本

你也可以创建一个批处理文件来快速启动：

创建文件 `启动网站.bat`：

```batch
@echo off
cd /d C:\Users\31600\Desktop\tcm-knowledge-graph\frontend
echo 正在启动开发服务器...
npm run dev
pause
```

双击运行即可自动启动网站。

---

## ⚠️ 常见问题

### Q: 提示 "npm 不是内部或外部命令"？
**A:** 需要先安装 Node.js：https://nodejs.org/

### Q: 端口被占用？
**A:** Vite 会自动尝试其他端口，或者你可以手动指定：
```powershell
npm run dev -- --port 3000
```

### Q: 页面显示空白？
**A:** 
1. 检查浏览器控制台是否有错误
2. 确认后端 API 服务是否正常运行
3. 检查网络请求是否成功

### Q: 需要同时运行后端吗？
**A:** 是的，如果网站需要连接后端 API，需要先启动后端服务：
```powershell
cd C:\Users\31600\Desktop\tcm-knowledge-graph\backend
npm install
npm run dev
```

---

## 🔗 相关链接

- **GitHub 仓库**: https://github.com/shaonayan/tcm-knowledge-graph
- **Node.js 下载**: https://nodejs.org/
- **Vite 文档**: https://vitejs.dev/

---

**提示**：首次运行需要安装依赖，可能需要几分钟时间。之后启动会很快。

