# 中医知识图谱Web应用 - 项目设置指南

## 🎉 项目结构创建完成！

您的中医知识图谱Web应用项目结构已经成功创建，包含以下内容：

```
tcm-knowledge-graph/
├── frontend/                   # React前端应用
│   ├── public/                # 静态资源
│   ├── src/                   # 源代码
│   │   ├── components/        # 组件目录
│   │   │   ├── common/        # 通用组件
│   │   │   ├── graph/         # 图谱组件
│   │   │   ├── search/        # 搜索组件
│   │   │   └── analytics/     # 分析组件
│   │   ├── pages/             # 页面目录
│   │   │   ├── Dashboard/     # 首页仪表板
│   │   │   ├── Explorer/      # 图谱探索器
│   │   │   ├── Search/        # 搜索页面
│   │   │   ├── Analytics/     # 数据分析
│   │   │   └── Learning/      # 学习模块
│   │   ├── hooks/             # 自定义Hooks
│   │   ├── services/          # API服务
│   │   ├── stores/            # 状态管理
│   │   ├── utils/             # 工具函数
│   │   ├── styles/            # 样式文件
│   │   └── types/             # 类型定义
│   ├── index.html             # 入口HTML
│   ├── package.json           # 依赖配置
│   ├── vite.config.ts         # Vite配置
│   ├── tsconfig.json          # TypeScript配置
│   └── tailwind.config.js     # Tailwind配置
├── backend/                    # Node.js后端API
│   ├── src/                   # 源代码
│   │   ├── controllers/       # 控制器
│   │   ├── services/          # 业务逻辑
│   │   ├── routes/            # 路由定义
│   │   ├── middleware/        # 中间件
│   │   ├── models/            # 数据模型
│   │   ├── utils/             # 工具函数
│   │   ├── config/            # 配置文件
│   │   └── types/             # 类型定义
│   ├── package.json           # 依赖配置
│   ├── tsconfig.json          # TypeScript配置
│   └── env.example            # 环境变量示例
├── docs/                      # 项目文档
├── docker-compose.yml         # Docker配置
├── .gitignore                 # Git忽略文件
└── README.md                  # 项目说明
```

## 🚀 下一步：安装开发环境

### 1. 安装Node.js和npm

**下载地址**: https://nodejs.org/

推荐安装LTS版本（18.x或更高版本）

安装完成后，重新打开命令行验证：
```bash
node --version
npm --version
```

### 2. 安装项目依赖

#### 前端依赖安装：
```bash
cd frontend
npm install
```

#### 后端依赖安装：
```bash
cd ../backend
npm install
```

### 3. 环境配置

#### 后端环境配置：
```bash
# 复制环境配置文件
copy env.example .env

# 编辑.env文件，配置以下信息：
# NEO4J_URI=bolt://localhost:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=您的Neo4j密码
# REDIS_HOST=localhost
# REDIS_PORT=6379
```

### 4. 数据库准备

#### Neo4j安装：
- **Neo4j Desktop**: https://neo4j.com/download/
- **Neo4j Aura** (云版本): https://neo4j.com/cloud/aura/

#### Redis安装：
- **Windows**: https://github.com/microsoftarchive/redis/releases
- **Docker**: `docker run -d -p 6379:6379 redis:alpine`

### 5. 启动开发服务器

#### 启动后端：
```bash
cd backend
npm run dev
```

#### 启动前端：
```bash
cd frontend
npm run dev
```

### 6. Docker部署（可选）

如果您安装了Docker：
```bash
# 在项目根目录运行
docker-compose up -d
```

## 📚 已创建的核心文件

### 前端核心文件：
- ✅ `package.json` - 依赖管理
- ✅ `vite.config.ts` - 构建配置
- ✅ `tsconfig.json` - TypeScript配置
- ✅ `tailwind.config.js` - 样式配置
- ✅ `index.html` - 入口页面
- ✅ `src/main.tsx` - 应用入口
- ✅ `src/App.tsx` - 主应用组件
- ✅ `src/styles/globals.css` - 全局样式

### 后端核心文件：
- ✅ `package.json` - 依赖管理
- ✅ `tsconfig.json` - TypeScript配置
- ✅ `src/app.ts` - 应用入口
- ✅ `env.example` - 环境配置示例

### 项目配置文件：
- ✅ `README.md` - 项目文档
- ✅ `.gitignore` - Git忽略配置
- ✅ `docker-compose.yml` - Docker配置

## 🎯 技术栈特色

### 前端技术栈：
- **React 18** + **TypeScript** - 现代化开发
- **Vite** - 快速构建工具
- **Ant Design** - 企业级UI组件
- **Tailwind CSS** - 实用优先的CSS框架
- **D3.js** + **Cytoscape.js** - 强大的图谱可视化
- **Zustand** - 轻量级状态管理
- **React Query** - 服务器状态管理

### 后端技术栈：
- **Node.js** + **Express** + **TypeScript** - 高性能API
- **Neo4j** - 图数据库
- **Redis** - 缓存系统
- **JWT** - 身份认证
- **Swagger** - API文档
- **Winston** - 日志系统

## 🔗 连接您的Neo4j数据

项目已经准备好连接您现有的中医知识图谱数据：
- 6,820个节点
- 43,542个关系
- 17个根分类

只需在后端环境配置中填入您的Neo4j连接信息即可。

## 📝 下一步开发计划

1. **环境搭建** - 安装Node.js和依赖
2. **数据连接** - 配置Neo4j连接
3. **API开发** - 创建图谱查询接口
4. **前端开发** - 实现可视化组件
5. **功能完善** - 添加搜索、分析等功能

## 💡 立即开始

现在您可以：

1. **安装Node.js** (如果还没有安装)
2. **进入项目目录并安装依赖**
3. **配置环境变量**
4. **启动开发服务器**

您的中医知识图谱Web应用框架已经准备就绪！🎊
