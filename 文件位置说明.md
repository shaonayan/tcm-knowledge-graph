# 📁 文件位置说明

## 🚀 部署相关文件位置

所有部署文件都在项目根目录：`C:\Users\31600\Desktop\tcm-knowledge-graph\`

### 一键部署脚本

**位置：** 项目根目录
- ✅ `一键部署.bat` - 自动构建和配置
- ✅ `启动生产服务器.bat` - 启动生产服务器
- ✅ `部署脚本.bat` - 部署准备脚本

### 部署文档

**位置：** 项目根目录
- 📄 `部署指南.md` - 完整部署文档（本地、云服务器、Docker）
- 📄 `README-部署.md` - 快速部署指南
- 📄 `快速部署说明.md` - 快速开始说明
- 📄 `部署完成检查清单.md` - 部署验证清单
- 📄 `文件位置说明.md` - 本文件

### 配置文件

**位置：** `backend\` 目录
- ⚙️ `backend\.env` - 后端环境变量（需要创建）
- ⚙️ `backend\env.example` - 环境变量示例
- ⚙️ `backend\.env.production.example` - 生产环境配置示例

**位置：** `frontend\` 目录
- ⚙️ `frontend\.env.production` - 前端生产环境配置

### 服务器文件

**位置：** `backend\` 目录
- 🔧 `backend\server-simple.js` - 主服务器文件（支持开发和生产环境）

### 构建输出

**位置：** `frontend\dist\` 目录
- 📦 前端构建后的文件（运行 `npm run build` 后生成）

## 📂 完整目录结构

```
tcm-knowledge-graph/
├── 一键部署.bat                    ← 双击运行，自动部署
├── 启动生产服务器.bat              ← 双击运行，启动服务器
├── 部署脚本.bat                    ← 部署准备脚本
├── 部署指南.md                     ← 完整部署文档
├── README-部署.md                  ← 快速部署指南
├── 快速部署说明.md                 ← 快速开始
├── 部署完成检查清单.md             ← 验证清单
├── 文件位置说明.md                 ← 本文件
│
├── frontend/                       ← 前端项目
│   ├── dist/                       ← 构建输出（部署后生成）
│   ├── src/                        ← 源代码
│   ├── package.json
│   └── .env.production             ← 前端生产配置
│
└── backend/                        ← 后端项目
    ├── server-simple.js            ← 主服务器文件
    ├── .env                         ← 环境变量（需要创建）
    ├── env.example                  ← 环境变量示例
    └── .env.production.example      ← 生产环境配置示例
```

## 🎯 快速开始

### 步骤1：找到部署脚本

在项目根目录找到：
- `一键部署.bat` （双击运行）

### 步骤2：配置环境变量

在 `backend\` 目录创建 `.env` 文件：
- 可以复制 `backend\env.example` 为 `.env`
- 或参考 `backend\.env.production.example`

### 步骤3：启动服务器

在项目根目录找到：
- `启动生产服务器.bat` （双击运行）

## 📍 当前项目位置

**项目根目录：**
```
C:\Users\31600\Desktop\tcm-knowledge-graph\
```

**或者：**
```
C:\Users\31600\Desktop\115558\
```
（如果您的项目在这个目录）

## 🔍 如何找到文件

### Windows资源管理器

1. 打开文件资源管理器
2. 导航到：`C:\Users\31600\Desktop\tcm-knowledge-graph\`
3. 您会看到所有部署脚本和文档

### 命令行

```bash
cd C:\Users\31600\Desktop\tcm-knowledge-graph
dir *.bat          # 查看所有批处理脚本
dir *.md           # 查看所有文档
```

## ✅ 验证文件是否存在

运行以下命令检查：

```bash
cd C:\Users\31600\Desktop\tcm-knowledge-graph

# 检查部署脚本
dir 一键部署.bat
dir 启动生产服务器.bat

# 检查文档
dir 部署指南.md
dir README-部署.md
```

## 🚀 立即开始部署

1. **打开项目文件夹**
   ```
   C:\Users\31600\Desktop\tcm-knowledge-graph\
   ```

2. **双击运行**
   ```
   一键部署.bat
   ```

3. **配置环境变量**
   - 编辑 `backend\.env` 文件

4. **启动服务器**
   - 双击运行 `启动生产服务器.bat`

5. **访问应用**
   - 打开浏览器：http://localhost:3001

